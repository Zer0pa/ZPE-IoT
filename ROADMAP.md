# Roadmap

## Current Proven Surface

1. managed preflight closes at `17 PASS / 0 FAIL / 1 DEFERRED`
2. strict DT closes at `27/27 PASS`
3. active E1 benchmark closes at `10/11` wins with `17.16x` mean CR
4. local arm64 native wheel cold-install proof is closed

## Before Public Publication

1. keep the private GitHub render synchronized with the approved local doc surface
2. preserve the March 21 authority block across root, docs, proofs, and audit routing
3. execute any publication-specific wheel/index workflow only after explicit owner ratification
4. decide whether a future benchmark wave resolves `DS-11` or keeps it frozen as an explicit boundary

## Deferred Engineering Work

- broader benchmark augmentation beyond the active E1 authority surface
- additional native wheel targets beyond the locally verified arm64 macOS path
- further compression uplift work that does not regress fidelity or provenance guarantees

## Deferred Until Explicit Owner Approval

- package publication
- public outreach
- visibility change to public
