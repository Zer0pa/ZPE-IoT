---
phase: 02-targeted-compression-uplift
plan: "01"
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - python/zpe_iot/_native.py
  - python/tests/test_native.py
  - python/tests/test_parity.py
  - validation/benchmarks/phase2_monotonicity_probe.py
  - proofs/artifacts/PHASE2_MONOTONICITY_PROBE_20260320.json
  - proofs/artifacts/PHASE2_WORKSTREAM_RECHECK_20260320.md
  - .gpd/phases/02-targeted-compression-uplift/02-01-SUMMARY.md
interactive: false

conventions:
  benchmark_surface: "unchanged real public E1"
  strict_gate_rule: "Any introduced mandatory DT failure rejects the candidate"
  native_boundary: "Python and Rust packet bytes must match under the same feature flags"
  monotonicity_surface: "DT-10 DS-02 threshold sweep under preset vibration"

contract:
  schema_version: 1
  scope:
    question: "After repairing the native packet boundary, do WI-1 and ZH-1 remain valid uplift candidates or collapse to DT-10-only failures?"
  claims:
    - id: claim-workstream-recheck
      statement: "A native-boundary parity repair plus a monotonicity recheck can determine whether WI-1 and ZH-1 still fail for multiple reasons or only for DT-10."
      deliverables: [deliv-native-wrapper-fix, deliv-monotonicity-probe, deliv-workstream-recheck]
      acceptance_tests: [test-wrapped-parity, test-dt11-recheck, test-workstream-ablation-recheck, test-monotonicity-probe]
      references: [ref-wi1-ablation-old, ref-zh1-ablation-old, ref-dt10, ref-dt11, ref-loss-diagnosis]
  deliverables:
    - id: deliv-native-wrapper-fix
      kind: code
      path: python/zpe_iot/_native.py
      description: "Native encode/decode path reuses the same WI-1 and ZH-1 outer wrappers as Python."
      must_contain:
        - "wrapped encode parity"
        - "wrapped decode unwrapping"
    - id: deliv-monotonicity-probe
      kind: data
      path: proofs/artifacts/PHASE2_MONOTONICITY_PROBE_20260320.json
      description: "Compact probe of DT-10 CR curves and full-stream wrapper compression inversions."
      must_contain:
        - "WI-1"
        - "ZH-1"
        - "monotonic_breaks"
        - "zlib_level_sweep"
    - id: deliv-workstream-recheck
      kind: report
      path: proofs/artifacts/PHASE2_WORKSTREAM_RECHECK_20260320.md
      description: "Bounded Phase 2 report for the native parity repair and workstream recheck."
      must_contain:
        - "DT-11 repaired"
        - "DT-10 only"
        - "structural monotonicity verdict"
  references:
    - id: ref-wi1-ablation-old
      kind: prior_artifact
      locator: validation/results/wi1_ablation_20260220T050410.json
      role: baseline
      why_it_matters: "Defines the pre-repair WI-1 gain and strict-gate rejection state."
      applies_to: [claim-workstream-recheck]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-zh1-ablation-old
      kind: prior_artifact
      locator: validation/results/zh1_ablation_20260219T030327.json
      role: baseline
      why_it_matters: "Defines the pre-repair ZH-1 gain and strict-gate rejection state."
      applies_to: [claim-workstream-recheck]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-dt10
      kind: spec
      locator: validation/destruct_tests/dt10_monotonicity.py
      role: gate
      why_it_matters: "Defines the strict monotonicity gate that still governs candidate retention."
      applies_to: [claim-workstream-recheck]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-dt11
      kind: spec
      locator: validation/destruct_tests/dt11_cross_platform.py
      role: gate
      why_it_matters: "Defines the parity gate repaired by the native boundary fix."
      applies_to: [claim-workstream-recheck]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-loss-diagnosis
      kind: prior_artifact
      locator: proofs/artifacts/LOSS_DIAGNOSIS.md
      role: context
      why_it_matters: "Carries forward the Phase 1 warning that WI-1 and ZH-1 are only relevant if strict gates stay green."
      applies_to: [claim-workstream-recheck]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [read, compare, cite]
  acceptance_tests:
    - id: test-wrapped-parity
      subject: claim-workstream-recheck
      kind: reproducibility
      procedure: "Run focused native and parity pytest coverage with experimental wrappers enabled."
      pass_condition: "Wrapped Python and Rust packets match and wrapped decode parity holds."
      evidence_required: [deliv-native-wrapper-fix]
      automation: automated
    - id: test-dt11-recheck
      subject: claim-workstream-recheck
      kind: reproducibility
      procedure: "Rerun DT-11 with WI-1 and ZH-1 flags enabled."
      pass_condition: "Both feature-flagged DT-11 runs pass with bit-identical packets."
      evidence_required: [deliv-native-wrapper-fix, ref-dt11]
      automation: automated
    - id: test-workstream-ablation-recheck
      subject: claim-workstream-recheck
      kind: consistency
      procedure: "Rerun WI-1 and ZH-1 ablations and compare strict-gate differentials against the pre-repair artifacts."
      pass_condition: "Post-repair candidate failures shrink to DT-10 only, with DT-11 removed from the rejection surface."
      evidence_required: [deliv-workstream-recheck, ref-wi1-ablation-old, ref-zh1-ablation-old]
      automation: hybrid
    - id: test-monotonicity-probe
      subject: claim-workstream-recheck
      kind: consistency
      procedure: "Inspect the monotonicity probe artifact for DT-10 breakpoints and zlib-level sweeps."
      pass_condition: "The artifact shows whether the monotonicity inversion survives simple compression-level tuning."
      evidence_required: [deliv-monotonicity-probe, ref-dt10]
      automation: hybrid
  forbidden_proxies:
    - id: fp-parity-fix-equals-retention
      subject: claim-workstream-recheck
      proxy: "Treating the DT-11 repair as sufficient to retain WI-1 or ZH-1."
      reason: "Strict-gate retention still requires DT-10 to remain green."
    - id: fp-zlib-level-tune
      subject: claim-workstream-recheck
      proxy: "Calling a simple zlib-level sweep a real uplift strategy once monotonicity still fails across levels."
      reason: "That would substitute tuning theater for the governing acceptance gate."
---

<objective>
Recheck the previously rejected packet-wrapper uplift candidates after repairing the native packet boundary.

Purpose: Determine whether WI-1 and ZH-1 remain viable Phase 2 candidates or collapse to DT-10-only failures.
Output: one native-boundary fix, one monotonicity probe artifact, one bounded recheck report, and one execution summary.
</objective>

<execution_context>
@/Users/prinivenpillay/.codex/get-physics-done/workflows/execute-plan.md
@/Users/prinivenpillay/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@proofs/artifacts/LOSS_DIAGNOSIS.md
@validation/results/wi1_ablation_20260220T050410.json
@validation/results/zh1_ablation_20260219T030327.json
@validation/destruct_tests/dt10_monotonicity.py
@validation/destruct_tests/dt11_cross_platform.py
@python/zpe_iot/codec.py
@python/zpe_iot/_native.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair native wrapper symmetry</name>
  <files>python/zpe_iot/_native.py</files>
  <action>Make native encode/decode and header parsing honor the same WI-1 and ZH-1 wrappers as the Python packet path.</action>
  <verify>Native encode must produce the same flagged packet bytes as Python encode, and native decode must unwrap before reading the packet header.</verify>
  <done>The native boundary no longer creates a DT-11 mismatch under WI-1 or ZH-1.</done>
</task>

<task type="auto">
  <name>Task 2: Add wrapped parity coverage</name>
  <files>python/tests/test_native.py, python/tests/test_parity.py</files>
  <action>Add focused tests for wrapped sample-count parsing, wrapped packet parity, and wrapped decode parity.</action>
  <verify>The targeted pytest run must pass with coverage disabled.</verify>
  <done>Wrapped parity is covered in the repo rather than only in ephemeral shell output.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the direct gates and ablations</name>
  <files>validation/results/wi1_ablation_20260320T204352.json, validation/results/zh1_ablation_20260320T210604.json</files>
  <action>Rerun DT-11 directly under WI-1 and ZH-1, then rerun both ablations to compare strict-gate differentials against the pre-repair artifacts.</action>
  <verify>Both DT-11 runs must pass and the ablation artifacts must show whether DT-10 is now the only introduced failure.</verify>
  <done>The post-repair rejection surface for WI-1 and ZH-1 is explicit rather than inferred.</done>
</task>

<task type="auto">
  <name>Task 4: Capture a bounded monotonicity verdict</name>
  <files>validation/benchmarks/phase2_monotonicity_probe.py, proofs/artifacts/PHASE2_MONOTONICITY_PROBE_20260320.json, proofs/artifacts/PHASE2_WORKSTREAM_RECHECK_20260320.md</files>
  <action>Implement a compact monotonicity probe, run it, and write a bounded report explaining whether simple wrapper retuning still has any standing.</action>
  <verify>The probe must preserve the DT-10 threshold curve and test whether the threshold 0.1 to 0.2 inversion survives zlib levels 1 through 9.</verify>
  <done>The report states clearly whether Phase 2 should keep tuning WI-1 and ZH-1 or pivot away from them.</done>
</task>

</tasks>
