---
phase: 02-targeted-compression-uplift
plan: "03"
type: execute
wave: 1
depends_on: ["02-02"]
files_modified:
  - validation/benchmarks/phase2_token_bitpack_feasibility.py
  - validation/benchmarks/phase2_token_bitpack_probe.py
  - proofs/artifacts/PHASE2_TOKEN_BITPACK_FEASIBILITY_20260321.json
  - proofs/artifacts/PHASE2_TOKEN_BITPACK_PROBE_20260321.json
  - .gpd/phases/02-targeted-compression-uplift/02-03-SUMMARY.md
  - python/zpe_iot/codec.py
  - python/tests/test_native.py
  - python/tests/test_parity.py
  - core/src/bitpack.rs
  - core/src/codec.rs
interactive: false

conventions:
  benchmark_surface: "unchanged real public E1"
  authority_gate: "mean CR >= 5.0x and PT-6 >= 7/8 with no DT regressions"
  mechanism_family: "exact-fidelity count-aware token bitpack"
  packet_compatibility: "legacy decode remains supported; new balanced/lossless encoding uses the reserved packet flag bit"

contract:
  schema_version: 1
  scope:
    question: "Can exact-fidelity count-aware token bitpacking materially improve the unchanged E1 authority surface without any fidelity, monotonicity, or cross-platform parity regression?"
  claims:
    - id: claim-token-bitpack-feasible
      statement: "The current balanced token stream is structurally wasteful because `count == 1` chunks dominate the payload, so one count-aware exact-fidelity bitpack attempt is justified before Phase 2 is downgraded."
      deliverables: [deliv-token-bitpack-feasibility]
      acceptance_tests: [test-token-bitpack-feasibility]
      references: [ref-bench-e1-20260320, ref-smooth-series-probe]
    - id: claim-token-bitpack-subset
      statement: "A count-aware bitpack can improve the decisive smooth-series subset without changing decode fidelity, breaking `DT-10`, or breaking Python/Rust parity."
      deliverables: [deliv-token-bitpack-probe, deliv-phase-summary]
      acceptance_tests: [test-dt11, test-dt10, test-token-bitpack-subset]
      references: [ref-bitpack-feasibility, ref-smooth-series-probe]
    - id: claim-token-bitpack-authority
      statement: "If the subset clears the guardrails, the same structural mechanism either moves the full E1 authority surface to the sovereign gate or produces an explicit bounded failure artifact."
      deliverables: [deliv-token-bitpack-probe, deliv-phase-summary]
      acceptance_tests: [test-authority-rerun-or-downgrade]
      references: [ref-bench-e1-20260320, ref-bitpack-feasibility]
  deliverables:
    - id: deliv-token-bitpack-feasibility
      kind: data
      path: proofs/artifacts/PHASE2_TOKEN_BITPACK_FEASIBILITY_20260321.json
      description: "Compact chunk-structure evidence showing whether count-aware exact-fidelity packing is large enough to justify implementation."
      must_contain:
        - "count1_chunk_share"
        - "subset_projection_from_authority"
        - "full_projection_from_authority"
    - id: deliv-token-bitpack-probe
      kind: data
      path: proofs/artifacts/PHASE2_TOKEN_BITPACK_PROBE_20260321.json
      description: "Measured subset and authority-surface verdict for the count-aware token bitpack line."
      must_contain:
        - "subset_results"
        - "dt10_pass"
        - "dt11_pass"
        - "authority_verdict"
    - id: deliv-phase-summary
      kind: report
      path: .gpd/phases/02-targeted-compression-uplift/02-03-SUMMARY.md
      description: "Bounded summary of whether the exact-fidelity bitpack revived or closed the wedge line."
      must_contain:
        - "exact-fidelity"
        - "subset verdict"
        - "authority verdict"
  references:
    - id: ref-bench-e1-20260320
      kind: authority_artifact
      locator: validation/results/bench_summary_E1_real_public_20260320T174720.json
      role: baseline
      why_it_matters: "Defines the sovereign March 20 benchmark surface and the acceptance gate Phase 2 must move or close honestly."
      applies_to: [claim-token-bitpack-feasible, claim-token-bitpack-authority]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-smooth-series-probe
      kind: prior_artifact
      locator: proofs/artifacts/PHASE2_SMOOTH_SERIES_PROBE_20260321.json
      role: disconfirmation
      why_it_matters: "Shows that the parameter-tuning line is closed because bytes improved while benchmark fidelity regressed."
      applies_to: [claim-token-bitpack-feasible, claim-token-bitpack-subset]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-bitpack-feasibility
      kind: prior_artifact
      locator: proofs/artifacts/PHASE2_TOKEN_BITPACK_FEASIBILITY_20260321.json
      role: ratification
      why_it_matters: "Shows that count-aware chunk statistics are strong enough to justify one bounded structural implementation attempt."
      applies_to: [claim-token-bitpack-subset, claim-token-bitpack-authority]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [read, compare, cite]
  acceptance_tests:
    - id: test-token-bitpack-feasibility
      subject: claim-token-bitpack-feasible
      kind: analysis
      procedure: "Measure chunk-level token structure on the current balanced stream and estimate the exact-fidelity payload savings from count-aware packing."
      pass_condition: "The artifact shows a dominant `count == 1` regime and a large enough probe-level projection to justify one bounded implementation attempt."
      evidence_required: [deliv-token-bitpack-feasibility, ref-bench-e1-20260320]
      automation: hybrid
    - id: test-dt11
      subject: claim-token-bitpack-subset
      kind: validation
      procedure: "Run wrapped and unwrapped Python/Rust parity checks after the packet-format change."
      pass_condition: "Python and Rust native packets remain bit-identical."
      evidence_required: [deliv-token-bitpack-probe]
      automation: hybrid
    - id: test-dt10
      subject: claim-token-bitpack-subset
      kind: validation
      procedure: "Run the monotonicity guardrail on the new balanced packet format."
      pass_condition: "`DT-10` remains green."
      evidence_required: [deliv-token-bitpack-probe]
      automation: hybrid
    - id: test-token-bitpack-subset
      subject: claim-token-bitpack-subset
      kind: benchmark
      procedure: "Rerun `DS-05`, `DS-02`, and `DS-08` with the count-aware bitpack while keeping quantisation, wrappers, and fidelity mode fixed."
      pass_condition: "At least two of the three datasets improve in CR with no fidelity regression and no DT regressions."
      evidence_required: [deliv-token-bitpack-probe, ref-smooth-series-probe]
      automation: hybrid
    - id: test-authority-rerun-or-downgrade
      subject: claim-token-bitpack-authority
      kind: benchmark
      procedure: "If the subset clears parity, monotonicity, and fidelity, rerun the full E1 benchmark. Otherwise issue a bounded downgrade verdict from the failed subset result."
      pass_condition: "The phase ends with either an updated E1 authority result or an explicit evidence-backed Phase 2 failure verdict."
      evidence_required: [deliv-token-bitpack-probe, deliv-phase-summary, ref-bench-e1-20260320]
      automation: hybrid
  forbidden_proxies:
    - id: fp-subset-only-victory
      subject: claim-token-bitpack-authority
      proxy: "Treating subset success as Phase 2 closure without either a full E1 rerun or an explicit downgrade."
      reason: "The sovereign gate is the unchanged full E1 surface."
    - id: fp-fidelity-blind-packing
      subject: claim-token-bitpack-subset
      proxy: "Claiming victory from smaller packets when decode fidelity, parity, or monotonicity changed."
      reason: "This plan is justified only because the candidate is exact-fidelity in principle; any regression collapses that premise."
---

<objective>
Execute one bounded structural uplift attempt for Phase 2.

Purpose: Replace the stale “one more smooth-series tweak” loop with an exact-fidelity count-aware token bitpack attempt, then decide honestly whether the authority benchmark line is revived or closed.
Output: one feasibility artifact, one measured probe artifact, one summary, and whichever authority verdict the measurements support.
</objective>

<execution_context>
@/Users/zer0pa-build/.codex/get-physics-done/workflows/execute-plan.md
@/Users/zer0pa-build/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/02-targeted-compression-uplift/02-RESEARCH.md
@proofs/artifacts/PHASE2_SMOOTH_SERIES_PROBE_20260321.json
@proofs/artifacts/PHASE2_TOKEN_BITPACK_FEASIBILITY_20260321.json
@validation/results/bench_summary_E1_real_public_20260320T174720.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement count-aware exact-fidelity packet packing</name>
  <files>python/zpe_iot/codec.py, core/src/bitpack.rs, core/src/codec.rs</files>
  <action>Add a count-aware bitstream for balanced and lossless packets that uses the reserved packet flag bit, preserves legacy decode compatibility, and leaves fast mode, thresholds, and wrappers unchanged.</action>
  <verify>The packet decoder must still accept legacy packets, and the new encoder must be representational only: quantisation and reconstruction are unchanged.</verify>
  <done>The structural candidate exists in both Python and Rust with a shared packet contract.</done>
</task>

<task type="auto">
  <name>Task 2: Re-establish parity and guardrails</name>
  <files>python/tests/test_native.py, python/tests/test_parity.py</files>
  <action>Extend the parity and native tests so the new packet path is covered unwrapped and wrapped, then run the decisive parity and monotonicity guardrails.</action>
  <verify>`DT-11` must stay green and `DT-10` must stay green under the new packet format.</verify>
  <done>The structural candidate is still admissible for authority benchmarking.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the decisive subset and authority surface</name>
  <files>validation/benchmarks/phase2_token_bitpack_probe.py, proofs/artifacts/PHASE2_TOKEN_BITPACK_PROBE_20260321.json</files>
  <action>Measure `DS-05`, `DS-02`, and `DS-08` first with the new packet format under the current benchmark fidelity mode. If the subset clears parity, monotonicity, and fidelity, rerun the full E1 benchmark; otherwise freeze a bounded failure verdict without widening the line.</action>
  <verify>The probe artifact must report the subset deltas, the guardrail outcomes, and either the authority rerun result or the reason the authority rerun was not justified.</verify>
  <done>The project knows whether exact-fidelity token bitpacking revives Phase 2 or closes it honestly.</done>
</task>

<task type="auto">
  <name>Task 4: Write the bounded verdict</name>
  <files>.gpd/phases/02-targeted-compression-uplift/02-03-SUMMARY.md</files>
  <action>Summarize whether the count-aware bitpack revived the benchmark wedge line or failed honestly, and state the exact next move for the project.</action>
  <verify>The summary must make the authority verdict explicit and forbid subset-only or packet-only success narratives.</verify>
  <done>Phase 2 can either close or hand off cleanly to the next phase without ambiguity.</done>
</task>

</tasks>
