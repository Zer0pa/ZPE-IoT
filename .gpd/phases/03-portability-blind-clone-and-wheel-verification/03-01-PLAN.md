---
phase: 03-portability-blind-clone-and-wheel-verification
plan: "01"
type: execute
wave: 1
depends_on: ["02-03"]
files_modified:
  - .gpd/phases/03-portability-blind-clone-and-wheel-verification/03-01-SUMMARY.md
  - proofs/artifacts/BLIND_CLONE_VERIFICATION.md
interactive: false

conventions:
  workspace_mode: "equivalently clean workspace under /tmp"
  authority_surface: "March 21, 2026 green E1 benchmark plus fresh strict DT"
  retention_policy: "keep only compact receipts in-repo; remove bulky temporary workspaces after capture"
---

<objective>
Verify that the current source tree can reproduce the essential install and validation surface in an equivalently clean workspace without hidden local dependencies.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Materialize an equivalently clean workspace</name>
  <files>proofs/artifacts/BLIND_CLONE_VERIFICATION.md</files>
  <action>Copy the current repo into a fresh temporary workspace under `/tmp`, excluding `.git`, release bundles, target outputs, dist artifacts, caches, and other rebuildable bulk.</action>
  <verify>The temporary workspace contains only the source and provenance inputs needed for installation and validation.</verify>
  <done>A clean-room copy exists without dragging historical local clutter into the evidence path.</done>
</task>

<task type="auto">
  <name>Task 2: Reproduce the essential install and validation surface</name>
  <files>proofs/artifacts/BLIND_CLONE_VERIFICATION.md</files>
  <action>Inside the temporary workspace, create a fresh virtualenv, install the Python package from source, run CLI smoke commands, and run the targeted DT subset that exercises parity, packet integrity, and provenance-sensitive behavior.</action>
  <verify>The install works in the clean workspace and the targeted validation subset passes there.</verify>
  <done>Phase 3 has a compact clean-workspace reproduction artifact rather than an assumption from the dirty local lane.</done>
</task>

</tasks>
