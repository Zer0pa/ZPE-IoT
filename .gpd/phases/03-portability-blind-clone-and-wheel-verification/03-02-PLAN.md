---
phase: 03-portability-blind-clone-and-wheel-verification
plan: "02"
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - .gpd/phases/03-portability-blind-clone-and-wheel-verification/03-02-SUMMARY.md
  - proofs/artifacts/WHEEL_VERIFICATION.md
interactive: false

conventions:
  build_mode: "wheel from python/pyproject.toml"
  install_mode: "fresh virtualenv under /tmp"
  retention_policy: "keep compact receipts and wheel path; delete temporary envs after capture"
---

<objective>
Verify that the current Python package builds a wheel and can be installed and exercised in a fresh environment without source-tree imports.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Build the wheel</name>
  <files>proofs/artifacts/WHEEL_VERIFICATION.md</files>
  <action>Build a wheel from `python/pyproject.toml` using the current Phase 2 code state.</action>
  <verify>A wheel file is produced successfully and the build path is recorded.</verify>
  <done>The package has a concrete distribution artifact rather than only source-tree execution.</done>
</task>

<task type="auto">
  <name>Task 2: Install and smoke-test the wheel in a cold environment</name>
  <files>proofs/artifacts/WHEEL_VERIFICATION.md</files>
  <action>Create a fresh virtualenv under `/tmp`, install the wheel, import `zpe_iot`, and run the CLI smoke path from the installed entrypoint.</action>
  <verify>The wheel install, import, and smoke commands all pass without relying on the source tree.</verify>
  <done>Phase 3 has a compact cold-wheel verification artifact rather than an implied packaging claim.</done>
</task>

</tasks>
