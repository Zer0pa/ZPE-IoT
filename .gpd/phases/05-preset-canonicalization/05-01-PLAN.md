---
phase: 05-preset-canonicalization
plan: "01"
type: execute
wave: 1
depends_on: ["04-02"]
files_modified:
  - .gpd/phases/05-preset-canonicalization/05-01-SUMMARY.md
  - python/zpe_iot/presets.py
  - core/src/presets.rs
  - python/tests/test_presets.py
  - python/tests/test_parity.py
  - python/tests/test_native.py
  - python/tests/test_packet_compatibility.py
interactive: false

conventions:
  authority_baseline: "March 21, 2026 green E1 + strict DT + bounded wedge verdict"
  parity_gate: "all 9 presets match across Rust and Python"
  acceptance_gate: "no DT-12, parity, or focused test regression"

contract:
  schema_version: 1
  scope:
    question: "Can the remaining Rust/Python preset drift be closed with current data and without regressing parity or preset floors?"
  claims:
    - id: claim-preset-ratification
      statement: "The drifted `temperature`, `gps_track`, and `flow` preset values can be ratified from measured current-surface evidence rather than guesswork."
      deliverables: [deliv-preset-summary]
      acceptance_tests: [test-preset-comparison]
      references: [ref-wave2-brief, ref-current-preset-surfaces]
    - id: claim-cross-language-parity
      statement: "Once the ratified values are applied, Rust and Python preset tables are canonical for all 9 presets and the focused parity surface remains green."
      deliverables: [deliv-preset-summary]
      acceptance_tests: [test-focused-parity]
      references: [ref-current-preset-surfaces]
  deliverables:
    - id: deliv-preset-summary
      kind: report
      path: .gpd/phases/05-preset-canonicalization/05-01-SUMMARY.md
      description: "Measured verdict for the drifted presets plus the focused verification surface."
      must_contain:
        - "winning values"
        - "focused tests"
        - "next action"
  references:
    - id: ref-wave2-brief
      kind: prior_artifact
      locator: proofs/artifacts/WAVE2_TECHNICAL_CLOSURE_BRIEF_20260321.md
      role: scope
      why_it_matters: "Defines Wave 2 as technical closure and forbids narratable wins."
      applies_to: [claim-preset-ratification]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [read, use, cite]
    - id: ref-current-preset-surfaces
      kind: code_surface
      locator: python/zpe_iot/presets.py + core/src/presets.rs
      role: current_state
      why_it_matters: "Contains the live preset drift that must be resolved."
      applies_to: [claim-preset-ratification, claim-cross-language-parity]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [read, compare, cite]
  acceptance_tests:
    - id: test-preset-comparison
      subject: claim-preset-ratification
      kind: benchmark
      procedure: "Compare the Rust and Python values for the drifted presets on the relevant current dataset mappings and retain only values that keep the fidelity floor while maximizing current-surface CR."
      pass_condition: "Each drifted preset has an explicit winning value and the losing surface is updated."
      evidence_required: [deliv-preset-summary]
      automation: hybrid
    - id: test-focused-parity
      subject: claim-cross-language-parity
      kind: validation
      procedure: "Run focused preset, parity, native, packet-compatibility, and Rust tests after the canonicalization change."
      pass_condition: "The focused parity surface is green after the preset update."
      evidence_required: [deliv-preset-summary]
      automation: hybrid
  forbidden_proxies:
    - id: fp-manual-alignment
      subject: claim-preset-ratification
      proxy: "Making the tables look aligned without a current-surface verification step."
      reason: "The point of Phase 5 is measured canonicalization, not cosmetic parity."
---

<objective>
Close preset drift before any new DS-05, packaging, or benchmark-breadth spend.

Purpose: choose and ratify the canonical values for the drifted presets, update the losing implementation surface, and prove that the focused parity floor remains green.
Output: one summary with the winning values, the applied code changes, and the focused verification verdict.
</objective>

<execution_context>
@<BUILD_HOME>/.codex/get-physics-done/workflows/execute-plan.md
@<BUILD_HOME>/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@proofs/artifacts/WAVE2_TECHNICAL_CLOSURE_BRIEF_20260321.md
@python/zpe_iot/presets.py
@core/src/presets.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the drifted preset values</name>
  <files>python/zpe_iot/presets.py, core/src/presets.rs</files>
  <action>Compare the current Rust and Python values for `temperature`, `gps_track`, and `flow` on the relevant dataset mappings. Keep only values that preserve the current fidelity floor while maximizing CR.</action>
  <verify>The decision for each drifted preset must be explicit and evidence-backed.</verify>
  <done>The winning values are known and the losing surface is identified.</done>
</task>

<task type="auto">
  <name>Task 2: Canonicalize the tables</name>
  <files>python/zpe_iot/presets.py, core/src/presets.rs</files>
  <action>Update the losing implementation surface so all 9 presets agree across Rust and Python.</action>
  <verify>There is one canonical value set and no residual table drift remains.</verify>
  <done>Rust and Python preset tables are structurally aligned.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the focused parity surface</name>
  <files>python/tests/test_presets.py, python/tests/test_parity.py, python/tests/test_native.py, python/tests/test_packet_compatibility.py</files>
  <action>Run the focused Python and Rust verification floor after canonicalization. If a test dependency is missing, install only what is required to make the focused surface runnable and record that setup step in the summary.</action>
  <verify>The focused parity surface must be green after the preset update.</verify>
  <done>The preset change is admissible for the rest of Wave 2.</done>
</task>

<task type="auto">
  <name>Task 4: Freeze the verdict</name>
  <files>.gpd/phases/05-preset-canonicalization/05-01-SUMMARY.md</files>
  <action>Write the ratified winning values, the verification result, and the next concrete task for Wave 2.</action>
  <verify>The summary states whether preset parity is now closed and names the next task without ambiguity.</verify>
  <done>Phase 5 has a bounded handoff surface.</done>
</task>

</tasks>
