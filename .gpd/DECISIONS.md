# Decisions

| Date | Decision | Rationale | Outcome |
| ---- | -------- | --------- | ------- |
| 2026-03-20 | Treat March 20 authority artifacts as sovereign | They supersede the older blocked-state release narrative | Active |
| 2026-03-20 | Start with baseline reconciliation and loss diagnosis before uplift work | Honest uplift depends on a frozen authority surface and explicit loss structure | Active |
| 2026-03-20 | Treat the Phase 2 blocker as a cross-dataset authority-gap problem, not a DS-01/DS-05-only problem | Matching the best current baselines on `DS-01` and `DS-05` would raise the March 20 E1 mean only to about `4.708x`, still below the sovereign `5.0x` gate | Active |
| 2026-03-20 | Narrow the next Phase 2 execution slice to byte-sink decomposition on the authority subset | Until `DS-05`, `DS-02`, `DS-06`, `DS-08`, and `DS-01` are measured for where bytes are actually being spent, every uplift idea is still unguided and risks another non-authoritative loop | Active |
| 2026-03-20 | Replace the `DS-02` single-bridge premise with code-backed bridge math | `proofs/artifacts/PHASE2_GAP_BUDGET_20260321.json` proves the March 20 E1 artifact supports no single winning bridge dataset; the smallest winning bridge set is `DS-02 + DS-06 + DS-08` if they each reach `5.0x` | Active |
| 2026-03-20 | Hard-gate Phase 2 plan 02-02 on a corrected smooth-series subset verdict | `proofs/artifacts/PHASE2_OVERHEAD_DECOMPOSITION_20260321.json` shows `DS-05`, `DS-02`, and `DS-08` share a smooth low-entropy payload sink family while `DS-06` does not, so the next real leverage is a bounded subset probe plus an early downgrade rule before any full E1 rerun | Active |
| 2026-03-20 | Force the bridge verdict to use packet-level byte accounting | The codec already exposes fixed header bytes, CRC bytes, optional wrapper bytes, and token structure, so the next honest blocker conversion must measure those directly before proposing mechanism work | Active |
| 2026-03-21 | Downgrade the smooth-series line if the subset gains do not leave a live route | `proofs/artifacts/PHASE2_SMOOTH_SERIES_PROBE_20260321.json` shows the smallest admissible subset candidate (`step=0.22`) is DT-safe and improves all three target datasets, but even the optimistic follow-on ceiling stays at `4.7465x`, so `DS-06` and a full rerun are not justified on this line | Active |
| 2026-03-21 | Replace the stale Phase 2 follow-on with exact-fidelity count-aware token bitpacking | `proofs/artifacts/PHASE2_TOKEN_BITPACK_FEASIBILITY_20260321.json` shows `count == 1` chunks dominate the balanced payload across the decisive subset and the wider E1 probe, making one representational-only bitpack attempt the only evidence-backed Phase 2 line left | Active |
| 2026-03-21 | Restore raw provenance inputs after aggressive cleanup before trusting new strict DT failures | `validation/results/dt_results_20260321T144533.json` failed only on `DT-17` and `DT-25` because raw E1 and Chemosense provenance artifacts had been deleted for disk recovery; rehydrating those inputs returned strict DT to `27 PASS` in `validation/results/dt_results_20260321T144919.json` | Active |
| 2026-03-21 | Treat clean-room native build and Chemosense summary generation as explicit portability steps, not hidden local assumptions | The first clean-workspace DT subset failed only because `_native` had not been built in that workspace and `DT-25` expected a local `bench_summary_chemosense_*.json`; once those steps were executed inside the clean room, the decisive subset passed | Active |
| 2026-03-21 | Close the wheel claim at pure-Python smoke rather than implying bundled native parity | The cold wheel installs and runs from `site-packages`, but `diagnostics` reports `native_available = false`, so the honest claim is wheel build/install/import smoke, not native-bundled distribution parity | Active |
